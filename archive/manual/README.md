# Manual Checks

This directory keeps one-off scripts that are useful for manual debugging but are not part of automated tests.

- `3dvqvae_smoke.py`: legacy VQ-VAE encode/decode smoke script. It contains a placeholder mesh path and should be edited before manual use.
